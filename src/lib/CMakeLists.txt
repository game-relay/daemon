# generated by cmake-generator
cmake_minimum_required(VERSION 3.16.3)

add_executable(game-relay-daemon daemon.c main.c)

include(Tornado.cmake)
set_tornado(game-relay-daemon)

target_include_directories(game-relay-daemon PRIVATE ../include)

target_link_libraries(game-relay-daemon PUBLIC relay-server-lib udp-server
                                               guise-client-udp)
